diff --git a/MASTER_CHECKLIST.md b/MASTER_CHECKLIST.md
index 90cf496..462f284 100644
--- a/MASTER_CHECKLIST.md
+++ b/MASTER_CHECKLIST.md
@@ -229,3 +229,27 @@
 - [x] Feature diagnostics are directly inspectable in the public UI.
 - [x] Reproducibility evidence chain is directly inspectable in the public UI.
 - [x] Final flagship M&A report remains separated as a real future research milestone rather than fabricated now.
+
+
+## v0.3 viewer-facing release gates
+
+- [x] Public-data showcase appears before the interactive explorer.
+- [x] Front-page Census-derived figures are programmatically validated against the release bundle.
+- [x] BLS QCEW context links to the official 2024 annual-averages release.
+- [x] Four-step viewer guide explains how to read the site.
+- [x] Methodology-at-a-glance appears near the front of the page.
+- [x] About section is present and avoids any 4.5 pickleball-level claim.
+- [x] About copy uses only previously documented projects/experience/sponsors; no invented awards, users, or outcomes.
+- [x] Self-contained `site_data.json.gz` allows branch-served GitHub Pages to initialize without CI-generated JSON files.
+- [x] Legacy generated-data fallback remains available for reproducible Actions deployments.
+- [x] A single missing optional asset can no longer prevent public-data/source sections from rendering.
+- [x] Data-load status is visible and accessible.
+- [x] Ranking is explicitly sorted before display.
+- [x] Query results are explicitly sorted before display.
+- [x] Responsive layouts cover public-data, guide, methodology preview, and About sections.
+- [x] JavaScript syntax passes.
+- [x] Compressed release bundle decompresses and parses successfully.
+- [x] 9,690 browser observations, 969 latest observations, 7,583 replay observations load from the compact bundle.
+- [x] End-to-end static/logic viewer workflow audit passes 49/49 checks.
+
+- [x] Research/data release validator passes 85/85 gates.
diff --git a/RELEASE_AUDIT.md b/RELEASE_AUDIT.md
index 80c59be..7e87cb0 100644
--- a/RELEASE_AUDIT.md
+++ b/RELEASE_AUDIT.md
@@ -40,3 +40,14 @@ The technical platform now includes:
 The current predictive outcome is two-year-ahead establishment growth, **not M&A activity**. The historical replay and rolling-origin diagnostics are additional establishment-growth evidence and are explicitly labeled exploratory/post-hoc where appropriate.
 
 The verified M&A transaction corpus, human-labeled AI extraction benchmark, final flagship transaction-level research report, outside expert review, real practitioner adoption, and independent recognition remain real future milestones in `RESEARCH_MILESTONES.md`; they are not fabricated or marked complete.
+
+
+## v0.3 viewer-facing audit
+
+The viewer-facing release was reworked after identifying a deployment mismatch: the HTML could be served directly from `main` while generated interactive JSON existed only in the Actions build. v0.3 adds a committed, deterministic compressed browser bundle (`data/site_data.json.gz`) plus legacy generated-data fallback.
+
+Additional checks cover front-page public-data visibility, exact validation of the three fixed Census-derived showcase figures, the 60-second guide, methodology preview, About section, data-status behavior, fallback messaging, responsive styling, and ranking/query ordering.
+
+**Automated research/data gate result: 85/85 passed.**
+
+**Viewer workflow audit: 49/49 passed.**
diff --git a/assets/app-core.js b/assets/app-core.js
index 1d9aeac..86fe23e 100644
--- a/assets/app-core.js
+++ b/assets/app-core.js
@@ -13,16 +13,69 @@ const sources=[
  {name:'SEC EDGAR',agency:'U.S. Securities and Exchange Commission',status:'Transaction-corpus source',desc:'Public filings and exhibits are a future source for human-verified acquisition-event extraction and benchmarking.',url:'https://www.sec.gov/edgar/search-and-access'}
 ];
 
-async function load(){
+function renderStaticScaffold(){
+ $('#heroMetrics').innerHTML=[['44,574','state-sector-year observations'],['51','states + D.C.'],['19','NAICS sectors'],['1978–2023','official BDS history']].map(x=>`<div class="metric"><b>${x[0]}</b><span>${x[1]}</span></div>`).join('');
+ $('#sourceGrid').innerHTML=sources.map(s=>`<article class="source-card"><span class="status">${s.status}</span><h3>${s.name}</h3><p><b>${s.agency}</b><br>${s.desc}</p><a href="${s.url}" target="_blank" rel="noreferrer">Open official source ↗</a></article>`).join('');
+}
+
+function unpackRows(pack,states={},sectors={}){
+ const keys=pack.k||[], rows=pack.r||[];
+ return rows.map(a=>{const o={};keys.forEach((k,i)=>o[k]=a[i]);if(o.st&&!o.state_name)o.state_name=states[o.st]||o.st;if(o.sector&&!o.sector_name)o.sector_name=sectors[o.sector]||o.sector;return o});
+}
+
+async function fetchGzipJSON(url){
+ const r=await fetch(url,{cache:'no-cache'}); if(!r.ok)throw new Error(`${url} (${r.status})`);
+ if(typeof DecompressionStream==='undefined')throw new Error('Browser does not support gzip stream decompression');
+ const stream=r.body.pipeThrough(new DecompressionStream('gzip'));
+ return JSON.parse(await new Response(stream).text());
+}
+
+async function loadSharded(){
+ const base='data/site/';
+ const mr=await fetch(base+'manifest.json',{cache:'no-cache'}); if(!mr.ok)throw new Error(`site manifest (${mr.status})`);
+ const m=await mr.json(), states=m.states||{}, sectors=m.sectors||{};
+ const get=async name=>{const r=await fetch(base+name,{cache:'no-cache'});if(!r.ok)throw new Error(`${name} (${r.status})`);return r.json()};
+ const [panelPacks,replayPacks,sensPack,research]=await Promise.all([
+   Promise.all(m.panel_files.map(get)), Promise.all(m.replay_files.map(get)), get(m.sensitivity_file), get(m.research_file)
+ ]);
+ PANEL=panelPacks.flatMap(x=>unpackRows(x,states,sectors));
+ LATEST=PANEL.filter(r=>r.year===2023).sort((a,b)=>(b.structural_readiness_score??-1)-(a.structural_readiness_score??-1));
+ REPLAY=replayPacks.flatMap(x=>unpackRows(x,states,sectors));
+ SENS=unpackRows(sensPack,states,sectors);
+ META=research.metadata||{}; VALID=research.validation||{}; DIAG=research.diagnostics||{}; REPRO=research.repro||{}; SOURCE=research.source||{};
+}
+
+async function loadBundled(){
+ const d=await fetchGzipJSON('data/site_data.json.gz');
+ const states=d.panel.states||{}, sectors=d.panel.sectors||{};
+ PANEL=unpackRows(d.panel,states,sectors);
+ LATEST=unpackRows(d.latest,states,sectors);
+ REPLAY=unpackRows(d.replay,states,sectors);
+ SENS=unpackRows(d.sensitivity,states,sectors);
+ META=d.metadata||{}; VALID=d.validation||{}; DIAG=d.diagnostics||{}; REPRO=d.repro||{}; SOURCE=d.source||{};
+}
+
+async function loadLegacy(){
  const paths=['data/panel_2014_2023.json','data/latest_2023.json','data/metadata.json','data/model_validation.json','data/score_sensitivity_2023.json','data/research_diagnostics.json','data/replay_2014_2021.json','data/reproducibility_manifest.json','data/source_manifest.json'];
- [PANEL,LATEST,META,VALID,SENS,DIAG,REPLAY,REPRO,SOURCE]=await Promise.all(paths.map(x=>fetch(x).then(r=>{if(!r.ok)throw new Error(x);return r.json()})));
+ [PANEL,LATEST,META,VALID,SENS,DIAG,REPLAY,REPRO,SOURCE]=await Promise.all(paths.map(x=>fetch(x,{cache:'no-cache'}).then(r=>{if(!r.ok)throw new Error(x);return r.json()})));
+}
+
+async function load(){
+ renderStaticScaffold();
+ let mode='compressed release bundle';
+ try{await loadBundled()}catch(bundleErr){
+   console.warn('Compressed release bundle unavailable; trying branch-safe shards.',bundleErr); mode='branch-safe shards';
+   try{await loadSharded()}catch(shardErr){console.warn('Branch-safe shards unavailable; trying generated release assets.',shardErr);mode='generated release assets';await loadLegacy()}
+ }
  renderStatic();initControls();renderAll();renderDiagnostics();renderRepro();
+ $('#dataStatus').textContent=`Interactive data ready · ${PANEL.length.toLocaleString()} browser observations · Census BDS through 2023 · ${mode}`;
+ $('#dataStatus').cla